const frase = "Ser um programador requer muita prática";
const palavras = frase.split(" ");

for (let i = 0; i < palavras.length; i++) {
    console.log(palavras[i]);
    
}

console.log(palavras.join(" "))